{
  "name": "myanili",
  "version": "1.0.1",
  "description": "MyAniLi",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "docker pull webdevops/php-apache-dev:7.4 && docker-compose build",
    "dev:build": "npm run build && npm run dev",
    "dev": "docker-compose up -d && docker exec -ti -w /frontend -u application myanili_app_1 npm run dev && docker-compose down",
    "shell": "docker exec -ti -w /frontend -u application myanili_app_1 bash"
  },
  "author": "Christopher Schreiner",
  "license": "ISC",
  "devDependencies": {
    "@commitlint/cli": "^9.1.2",
    "@commitlint/config-conventional": "^10.0.0",
    "@elunic/ecs": "^0.3.3",
    "commitlint": "^11.0.0",
    "husky": "^4.3.7",
    "lint-staged": "^10.5.3",
    "prettier": "^2.2.1",
    "tslint": "^6.1.3",
    "typescript": "^4.1.3"
  }
}
