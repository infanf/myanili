import { DarkTableDirective } from './dark-table.directive';

describe('DarkTableDirective', () => {
  it('should create an instance', () => {
    const directive = new DarkTableDirective();
    expect(directive).toBeTruthy();
  });
});
